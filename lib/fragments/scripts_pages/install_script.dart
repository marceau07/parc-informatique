import 'package:flutter/material.dart';

class InstallScript extends StatelessWidget {
  const InstallScript({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return const Center(
      child: Text("Installer un script"),
    );
  }

}